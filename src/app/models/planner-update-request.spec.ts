import { PlannerUpdateRequest } from './planner-update-request';

describe('PlannerUpdateRequest', () => {
  it('should create an instance', () => {
    expect(new PlannerUpdateRequest()).toBeTruthy();
  });
});
