import { PlannerRequestJual } from './planner-request-jual';

describe('PlannerRequestJual', () => {
  it('should create an instance', () => {
    expect(new PlannerRequestJual()).toBeTruthy();
  });
});
