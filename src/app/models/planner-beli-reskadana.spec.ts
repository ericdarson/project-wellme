import { PlannerBeliReskadana } from './planner-beli-reskadana';

describe('PlannerBeliReskadana', () => {
  it('should create an instance', () => {
    expect(new PlannerBeliReskadana()).toBeTruthy();
  });
});
